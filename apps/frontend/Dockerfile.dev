# Frontend Development Dockerfile with Vite hot reload
FROM node:20-alpine

# Install pnpm
RUN corepack enable && corepack prepare pnpm@10 --activate

WORKDIR /app

COPY package.json ./

# Install without lockfile for dev (lockfile is at monorepo root)
RUN pnpm install --no-frozen-lockfile

COPY . .

EXPOSE 3000

CMD ["pnpm", "dev", "--host", "0.0.0.0"]
















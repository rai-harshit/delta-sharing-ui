# ====================================
# Delta Sharing UI Configuration
# ====================================

# Server Configuration
PORT=5000
NODE_ENV=development
LOG_LEVEL=info

# Delta Sharing Endpoint (used for generating recipient credentials)
# Set this to the public URL where recipients will connect
DELTA_SHARING_ENDPOINT=http://localhost:5000/api/delta

# ====================================
# Database (PostgreSQL - required)
# ====================================
# Connection URL with optional pooling parameters:
# ?connection_limit=10&pool_timeout=30
DATABASE_URL=postgresql://postgres:password@localhost:5432/delta_ui?connection_limit=10&pool_timeout=30
POSTGRES_USER=delta_sharing
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=delta_sharing

# ====================================
# Security (required)
# ====================================

# JWT secret for admin authentication (generate with: openssl rand -hex 32)
JWT_SECRET=change-this-to-a-secure-random-string

# Encryption key for storing cloud credentials (generate with: openssl rand -hex 32)
# IMPORTANT: Do not lose this key - encrypted credentials cannot be recovered without it
ENCRYPTION_KEY=generate-with-openssl-rand-hex-32

# CORS origin (frontend URL)
CORS_ORIGIN=http://localhost:3000

# ====================================
# Frontend Configuration
# ====================================
VITE_API_URL=http://localhost:5000

# ====================================
# Cloud Storage (Optional)
# These are fallback credentials if not configured via the UI
# ====================================

# --- AWS S3 ---
# Used for accessing Delta tables stored in S3
# You can also configure these via the Storage Settings UI
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
# For S3-compatible storage (MinIO, LocalStack, Cloudflare R2):
# AWS_S3_ENDPOINT=http://localhost:9000

# --- Azure Blob Storage ---
# Option 1: Connection string (easiest)
AZURE_STORAGE_CONNECTION_STRING=
# Option 2: Account + Key (alternative to connection string)
# AZURE_STORAGE_ACCOUNT=mystorageaccount
# AZURE_STORAGE_KEY=

# --- Google Cloud Storage ---
# Path to your GCP service account JSON key file
# The file will be mounted into the container at /app/gcp-key.json
GCP_KEY_FILE=
# Alternatively, set the project ID if using application default credentials
# GOOGLE_CLOUD_PROJECT=my-gcp-project
